<!DOCTYPE html>
<html lang="en">
  
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Image Uploading</title>
</head>
<body>
    <div>
<table>
  <tr>
    <td>Name</td>
    <td>Category</td>
    <td>Seller</td>
    <td>Availabel Unit</td>
    <td>Status</td>
    <%if(item.isApproved){%>
    <td>Price</td>
    <td>Unit</td>
    <%}%>
    <td>Image</td>
    <td>link</td>
  </tr>
    <tr>
      <td><%=item.name%></td>
      <td><%=item.category.name%></td>
      <td><%=item.seller.fullName%></td>
      <td><%=item.availableUnit%></td>

      <% if (item.isApproved) {%>
      <td>Approved</td>
      <td><%=item.sellPrice%></td>
      <td><%=item.unit.value%> Kg</td>
      <%}else{%>
      <td>Pending</td>
      <%}%>
      <td><img src = "<%=item.image.img%>" width=100% height=100%></img></td>
      <td>items/view/<%=cipherTextItem%>/<%=cipherTextEmail%></td>
    </tr>
</table>
</div>
</body>
</html>